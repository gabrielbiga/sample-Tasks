<Page xmlns="http://www.nativescript.org/tns.xsd"
      loaded="pageLoaded"
      navigatedTo="navigatedTo" >
  <Page.optionsMenu>
    <MenuItem text="{{ strings.done }}" tap="doneMenuItemTap" android.position="actionBar" />
  </Page.optionsMenu>
  <GridLayout>
    <ListView items="{{ items }}" itemTap="itemTap" >
      <ListView.itemTemplate>
        <GridLayout cssClass="itemContainer">
          <Label text="{{ data.Name }}" cssClass="{{ isSelected ? 'item-selected' : 'item' }}" />
        </GridLayout>
      </ListView.itemTemplate>
    </ListView>

    <ActivityIndicator busy="{{ isLoading }}" />
  </GridLayout>
</Page>